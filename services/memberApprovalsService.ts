export const memberApprovalsTable = "member_approvals";

export const memberApprovalsService = {
  list: async () => [],
  submit: async (id: number) => id,
  approve: async (id: number) => id,
  reject: async (id: number) => id,
  requestCorrection: async (id: number) => id,
};
